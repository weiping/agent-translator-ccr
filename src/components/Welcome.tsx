import React from 'react';
import { Box } from 'ink';
import BigText from 'ink-big-text';

export const Welcome = () => {
    return (
        <Box>
            <BigText text="Translator Agent" />
        </Box>
    );
};
